-- ============================================================
-- مدارك النخبة v2 — Migration 47
-- Revert SQL 46 — المحاولة لم تنجح
-- ============================================================
-- ملاحظة: SQL 40-42 (mock_exam) لا تُمسّ — هي تحلّ مشاكل
-- المسابقة، رجوعها يكسر المسابقة بدون فائدة للوحة.
--
-- بعد التشغيل:
--   - الوضع يطابق نهاية SQL 45 بالضبط
--   - التطبيق العادي شغّال
--   - المسابقة شغّالة
--   - الأدمن يدخل اللوحة (الأرقام صفر — مثل أمس)
-- ============================================================

DROP POLICY IF EXISTS "profiles_admin_read" ON public.profiles;
